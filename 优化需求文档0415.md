# 🎨 MyDay UI/UX 优化需求文档

我需要你帮我进行全面的 UI/UX 美化和代码重构，使其看起来更具现代感、更精致。

请按照以下几个维度逐步修改我的代码：

## 1. 全局色彩与视觉层次优化 (Global Styles)

- **背景色调整**：将全局主背景色从纯白或冷灰改为更温暖柔和的浅色（例如 `bg-slate-50`），让纯白色的卡片（`bg-white`）能更好地凸显出来。
- **卡片样式重构**：弱化所有卡片的边框。将现有的边框改为极浅的颜色（如 `border border-slate-100`），并增加柔和的弥散阴影（如 `shadow-sm` 或 `shadow-[0_2px_10px_-3px_rgba(6,81,237,0.1)]`），同时将圆角增大到 `rounded-2xl`，营造“悬浮感”。
- **统一主题色**：确保全局的主色调统一为现代感更强的蓝色（如 Tailwind 的 `blue-500` 到 `blue-600`）。移除图表中突兀的高饱和绿色，改用与主色调协调的颜色。

## 2. 核心组件交互改造 (Core Components)

- **图表美化（数据统计页面 - Recharts）**：
  - 将“每日评分趋势”的折线图 (`LineChart`) 改为 **面积图 (`AreaChart`)**。
  - 在面积图下方添加与线条同色的 **半透明渐变填充 (Gradient Fill)**。
  - 将背景网格线 (`CartesianGrid`) 改为浅色的虚线（如 `stroke="#f1f5f9" strokeDasharray="3 3"`），去掉垂直网格线。
  - 隐藏折线上的默认数据圆点，改为仅在鼠标 Hover 时显示 (ActiveDot)。
- **日历视图（历史记录页面）**：
  - 参考 GitHub 的热力图（Heatmap）风格。给有打卡记录的日期单元格加上极浅的蓝色背景（如 `bg-blue-50`），分数越高背景色稍微加深，让历史状态一目了然。

## 3. 排版与留白 (Typography & Whitespace)

- **空状态 (Empty States)**：
  - 优化“暂无任务”的显示效果。去掉生硬的虚线框，居中显示一个色彩柔和的 SVG 图标（如空盒子或咖啡杯，可使用 `lucide-react`），并配上鼓励性的文案，如：“今天也是充满希望的一天，添加你的第一个任务吧！”。
- **数字字体层级**：
  - 针对统计卡片中的核心数据（如“6 分”、“2 项”、“7 天”），使用更大的字号（`text-4xl`）和更重的字重（`font-extrabold` 或 `font-black`），并适当收紧字间距（`tracking-tight`），增强视觉冲击力。

## 4. 交互与动效 (Interactions & Animations)

- **Hover 态反馈**：给所有可点击的卡片、按钮和任务项增加过渡动画（`transition-all duration-300`）。鼠标悬浮时，卡片可以微微上浮（`-translate-y-1`）并加深阴影（`hover:shadow-md`）。

---

**执行要求**：
请确认你理解了上述需求。你可以分步骤向我提供修改后的代码，优先从“全局卡片样式”和“图表美化”开始。